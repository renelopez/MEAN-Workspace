.panel.panel-primary
    .panel-heading.text-center Featured Courses
    .panel-body
        .row(ng-repeat="course in vm.courses | filter:{featured:course.featured}")
            .col-md-12 {{course.title}}