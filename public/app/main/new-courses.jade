.panel.panel-primary
   .panel-heading.text-center New Courses
   .panel-body
      .panel-body
         .row(ng-repeat="course in vm.courses | orderBy:'published' | limitTo:10")
            .col-md-3 {{course.published | date:'MMM d yyyy'}}
            .col-md-9 {{course.name}}
